const firstName: string = 'Luke';
const lastName = 'Skywalker';

console.log(`Hi, my name is ${firstName} ${lastName}`);

/**
 * Run tsc app.ts ==outDir dist to write the output of the compiler to the dist folder
 */